name: Build Container Mavp2p

on:
    workflow_dispatch:
    pull_request:
        branches:
            - main
            - develop
    push:
        branches:
            - main

jobs:
    build:
        uses: bellflight/github-actions/avr_build_and_push_container.yml@main
        with:
          container_name: mavp2p